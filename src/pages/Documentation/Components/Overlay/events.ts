import { EventDefinitions } from '@kitchen/component/Events';

export const events: EventDefinitions = {
  click: {
    description: 'Emitted when clicking on the overlay',
    payload: undefined,
  },
};
